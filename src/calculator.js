export default class Calculator {
  add(x, y){
    return parseInt(x + y);
  }

  subtract(x, y){
    return parseInt(x - y);
  }

  multiply(x, y){
    return parseInt(x * y);
  }

  divide(x, y){
    return parseInt(x / y);
  }
}
